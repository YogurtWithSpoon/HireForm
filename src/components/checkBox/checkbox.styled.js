import styled from 'styled-components';

export const StyledCheckBox = styled.input`
  opacity: 0;
  position: absolute;
  width: 1rem;
  height: 1rem;
  z-index: 1;
  :hover{
    cursor: pointer;
  }
`