import styled from 'styled-components';

export const ChipContainer = styled.div`
  display:flex;
  justify-content: space-between;
  align-items: center;
  background: rgba(158, 157, 175, 0.13);
  height: 21px;
  border-radius: 3rem;
  padding: 3px 5px 3px 4px;
`